<?php View::content() ?> 
<h3>Editar registro de la tabla Abogado<h3>
<?= Form::open(); // por defecto llama a la misma url ?>
  <label>Tipo de Acceso <?= Form::text('acceso.tipo') ?></label>
  <?= Form::hidden('acceso.id') ?> 
  <?= Form::submit('Actualizar') ?> 
  
<?= Form::close() ?>
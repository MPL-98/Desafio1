<?php View::content() ?>
<h3>Crear un nuevo registro en la tabla Acceso<h3>

<?= Form::open(); // por defecto llama a la misma url ?> 

      <label>Tipo de acceso
      <?= Form::text('acceso.tipo') ?></label>

      

      <?= Form::submit('Agregar') ?> 
      

<?= Form::close() ?> 
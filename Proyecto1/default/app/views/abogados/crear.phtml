<?php View::content() ?>
<h3>Crear un nuevo registro en la tabla Abogado<h3>

<?= Form::open(); // por defecto llama a la misma url ?> 

      <label>Nombre
      <?= Form::text('abogados.nombre') ?></label>

      

      <?= Form::submit('Agregar') ?> 
      

<?= Form::close() ?> 
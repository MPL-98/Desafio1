<?php View::content() ?>
<h3>Agregar un nuevo registro a Abogados<h3>

<?= Form::open(); // por defecto llama a la misma url ?> 

      <label>Nombre
      <?= Form::text('Abogados.Nombre') ?></label>

      <?= Form::submit('Agregar') ?> 

<?= Form::close() ?>